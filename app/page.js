"use client";
import {useEffect,useMemo,useState} from "react";

const blank={id:null,name:"",recipe_type:"menu",category:"",status:"draft",yield_quantity:"",yield_unit:"g",prep_time_minutes:"",cook_time_minutes:"",kitchen_notes:"",components:[],steps:[],photos:[],versions:[]};
const wizard=[["basics","Basics","Recipe identity"],["ingredients","Ingredients","What goes in"],["method","Method","How it’s made"],["finish","Finish","Yield, photos & notes"]];
const photoTypes=[["prep","Prep"],["during","During"],["dine_in","Dine-in"],["takeaway","Takeaway"],["delivery","Delivery"],["final","Final"]];

export default function Home(){
 const [data,setData]=useState({ingredients:[],categories:[],recipes:[]});
 const [view,setView]=useState("home"),[filter,setFilter]=useState("all"),[q,setQ]=useState("");
 const [recipe,setRecipe]=useState(null),[step,setStep]=useState("basics"),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false);
 const [addingIngredient,setAddingIngredient]=useState(false),[addingCategory,setAddingCategory]=useState(false),[priceItem,setPriceItem]=useState(null);
 const [timerStart,setTimerStart]=useState(null),[elapsed,setElapsed]=useState(0);

 async function load(){setLoading(true);const r=await fetch('/api/bootstrap',{cache:'no-store'});const j=await r.json();setData(j.error?{ingredients:[],categories:[],recipes:[]}:j);setLoading(false)}
 useEffect(()=>{load();if('serviceWorker' in navigator)navigator.serviceWorker.register('/sw.js').catch(()=>{})},[]);
 useEffect(()=>{if(!timerStart)return;const t=setInterval(()=>setElapsed(Math.floor((Date.now()-timerStart)/1000)),1000);return()=>clearInterval(t)},[timerStart]);
 const fmt=s=>`${String(Math.floor(s/60)).padStart(2,'0')}:${String(s%60).padStart(2,'0')}`;
 const filteredRecipes=useMemo(()=>data.recipes.filter(r=>{const hit=(r.name+' '+(r.category||'')).toLowerCase().includes(q.toLowerCase());if(!hit)return false;if(filter==='menu')return r.recipe_type==='menu';if(filter==='bulk')return r.recipe_type==='bulk';if(filter==='draft')return r.status==='draft';return true}),[data.recipes,q,filter]);
 const filteredIngredients=useMemo(()=>data.ingredients.filter(i=>i.name.toLowerCase().includes(q.toLowerCase())),[data.ingredients,q]);
 const filteredCategories=useMemo(()=>data.categories.filter(c=>c.name.toLowerCase().includes(q.toLowerCase())),[data.categories,q]);
 const titles={all:['All Recipes','Every menu item and bulk preparation.'],menu:['Menu Recipes','Customer-facing dishes only.'],bulk:['Bulk Recipes','Prep recipes used inside other recipes.'],draft:['My Drafts','Recipes still being worked on.']};

 function navRecipes(f){setFilter(f);setView('recipes');setQ('')}
 function startRecipe(type='menu'){setRecipe({...blank,recipe_type:type,components:[],steps:[],photos:[],versions:[]});setStep('basics');setElapsed(0);setTimerStart(null)}
 async function openRecipe(id){const r=await fetch(`/api/recipes/${id}`,{cache:'no-store'});const j=await r.json();if(!r.ok)return alert(j.error||'Could not load recipe');setRecipe({...blank,...j,components:(j.components||[]).map(c=>({kind:c.bulk_recipe_id?'bulk':'ingredient',id:c.bulk_recipe_id||c.ingredient_id,name:c.bulk_recipe_name||c.ingredient_name,quantity:Number(c.quantity),unit:c.unit,notes:c.notes||''})),steps:(j.steps||[]).map(s=>({instruction:s.instruction,duration_seconds:s.duration_seconds})),photos:j.photos||[],versions:j.versions||[]});setStep('basics')}
 async function saveRecipe(forceStatus){if(!recipe.name.trim()){setStep('basics');return alert('Recipe name is required')}if(!recipe.category){setStep('basics');return alert('Please select a category')}setSaving(true);const body={...recipe,status:forceStatus||recipe.status};const r=await fetch(recipe.id?`/api/recipes/${recipe.id}`:'/api/recipes',{method:recipe.id?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const j=await r.json();setSaving(false);if(!r.ok)return alert(j.error||'Could not save recipe');setRecipe(null);await load();navRecipes(body.recipe_type==='bulk'?'bulk':'menu')}
 async function saveIngredient(e){e.preventDefault();const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/ingredients',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const j=await r.json();if(!r.ok)return alert(j.error);setAddingIngredient(false);await load()}
 async function saveCategory(e){e.preventDefault();const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/categories',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const j=await r.json();if(!r.ok)return alert(j.error);setAddingCategory(false);await load();if(recipe&&!recipe.category)setRecipe(x=>({...x,category:j.name}))}
 async function savePrice(e){e.preventDefault();const body=Object.fromEntries(new FormData(e.currentTarget));body.ingredient_id=priceItem.id;const r=await fetch('/api/prices',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const j=await r.json();if(!r.ok)return alert(j.error);setPriceItem(null);await load()}
 function addComp(){const raw=document.getElementById('comp').value,qty=document.getElementById('qty').value,unit=document.getElementById('unit').value;if(!raw||!qty)return;const isBulk=raw.startsWith('bulk:'),id=raw.replace('bulk:','');const src=isBulk?data.recipes.find(x=>x.id===id):data.ingredients.find(x=>x.id===id);setRecipe({...recipe,components:[...recipe.components,{kind:isBulk?'bulk':'ingredient',id,name:src.name,quantity:Number(qty),unit}]});document.getElementById('qty').value=''}
 function addStep(){const instruction=document.getElementById('methodText').value.trim(),seconds=document.getElementById('methodSecs').value;if(!instruction)return;setRecipe({...recipe,steps:[...recipe.steps,{instruction,duration_seconds:seconds?Number(seconds):null}]});document.getElementById('methodText').value='';document.getElementById('methodSecs').value=''}
 async function addPhoto(e,type){const f=e.target.files?.[0];if(!f)return;const reader=new FileReader();reader.onload=()=>setRecipe(x=>({...x,photos:[...(x.photos||[]).filter(p=>p.photo_type!==type),{photo_type:type,storage_key:'inline:'+Date.now(),public_url:reader.result,caption:''}]}));reader.readAsDataURL(f)}
 const badge=r=><span className={`pill ${r.status||'draft'}`}>{r.status==='approved'?'Approved':r.status==='recorded'?'Recorded':'Draft'}</span>;
 const recipeCard=r=><button className="list-card" key={r.id} onClick={()=>openRecipe(r.id)}><div className={`list-icon ${r.recipe_type}`}>{r.recipe_type==='bulk'?'B':'M'}</div><div><b>{r.name}</b><span>{r.category||'Uncategorized'} · v{r.version_no||1} · {r.component_count||0} items</span></div>{badge(r)}<strong>›</strong></button>;

 const Sidebar=()=> <aside className="sidebar"><div className="side-brand"><div className="flame">◆</div><div><b>SALTED<br/>GRILLS</b><span>RECIPE LAB</span></div></div><nav className="side-nav"><button className={view==='home'?'active':''} onClick={()=>setView('home')}><i>⌂</i>Dashboard</button><div className="nav-label">RECIPES</div><button className={view==='recipes'&&filter==='all'?'active':''} onClick={()=>navRecipes('all')}><i>▣</i>All Recipes</button><button className={view==='recipes'&&filter==='menu'?'active':''} onClick={()=>navRecipes('menu')}><i>♨</i>Menu Recipes</button><button className={view==='recipes'&&filter==='bulk'?'active':''} onClick={()=>navRecipes('bulk')}><i>▤</i>Bulk Recipes</button><button className={view==='recipes'&&filter==='draft'?'active':''} onClick={()=>navRecipes('draft')}><i>♧</i>My Drafts</button><div className="nav-label">MASTER DATA</div><button className={view==='ingredients'?'active':''} onClick={()=>{setView('ingredients');setQ('')}}><i>♨</i>Ingredients</button><button className={view==='categories'?'active':''} onClick={()=>{setView('categories');setQ('')}}><i>◫</i>Categories</button><div className="nav-label">COSTING</div><button className={view==='costing'?'active':''} onClick={()=>{setView('costing');setQ('')}}><i>₨</i>Ingredient Prices</button></nav><div className="side-user"><div>AY</div><p><b>Asfand Yar</b><span>Admin</span></p></div></aside>;

 if(recipe){const idx=wizard.findIndex(x=>x[0]===step);return <div className="layout"><Sidebar/><main className="main-shell"><section className="editor-page"><header className="editor-top"><div className="editor-title"><button onClick={()=>setRecipe(null)}>‹</button><div><h1>{recipe.id?'Edit Recipe':'New Recipe'}</h1><p>{recipe.id?'Update this recipe without losing its history':'Create a new recipe step by step'}</p></div></div><div className="editor-actions"><button className="draft-btn" onClick={()=>saveRecipe('draft')}>▣ <span>Save as Draft</span></button>{badge(recipe)}<button className="close-x" onClick={()=>setRecipe(null)}>×</button></div></header><div className="wizard"><div className="wizard-line" style={{'--progress':`${(idx/(wizard.length-1))*100}%`}}></div>{wizard.map(([k,l,s],i)=><button key={k} className={step===k?'active':i<idx?'done':''} onClick={()=>setStep(k)}><i>{i+1}</i><p><b>{l}</b><span>{s}</span></p></button>)}</div><div className="editor-scroll"><div className="editor-content">{step==='basics'&&<><div className="section-kicker">STEP 1 OF 4</div><h2>What are we preparing?</h2><p className="section-copy">Start with the identity of the recipe. Nothing financial is required here.</p><div className="type-grid"><button className={recipe.recipe_type==='menu'?'active':''} onClick={()=>setRecipe({...recipe,recipe_type:'menu'})}><div className="type-icon menu">♨</div><div><b>Menu Item</b><span>Sold directly to a customer</span><small>Example: Chicken Fried Rice</small></div><i>{recipe.recipe_type==='menu'?'✓':''}</i></button><button className={recipe.recipe_type==='bulk'?'active':''} onClick={()=>setRecipe({...recipe,recipe_type:'bulk'})}><div className="type-icon bulk">▤</div><div><b>Bulk Recipe</b><span>Used inside other recipes</span><small>Example: Boiled Rice</small></div><i>{recipe.recipe_type==='bulk'?'✓':''}</i></button></div><div className="form-field"><label>Recipe Name <em>*</em></label><input value={recipe.name} onChange={e=>setRecipe({...recipe,name:e.target.value})} placeholder="e.g. Chicken Fried Rice"/></div><div className="form-field"><div className="label-row"><label>Category <em>*</em></label><button className="mini-link" onClick={()=>setAddingCategory(true)}>＋ Add category</button></div><select value={recipe.category||''} onChange={e=>setRecipe({...recipe,category:e.target.value})}><option value="">Select a category</option>{data.categories.map(c=><option key={c.id} value={c.name}>{c.name}</option>)}</select></div></>}{step==='ingredients'&&<><div className="section-kicker">STEP 2 OF 4</div><h2>What goes into it?</h2><p className="section-copy">Add raw ingredients or a prepared bulk recipe with the exact quantity used.</p><div className="components">{recipe.components.map((c,n)=><div className="component-row" key={n}><i>{n+1}</i><div><b>{c.name}</b><span>{c.kind==='bulk'?'Bulk recipe':'Ingredient'}</span></div><strong>{c.quantity} {c.unit}</strong><button onClick={()=>setRecipe({...recipe,components:recipe.components.filter((_,i)=>i!==n)})}>×</button></div>)}</div><div className="entry-card"><div className="form-field"><label>Ingredient or Bulk Recipe</label><select id="comp"><option value="">Select an item</option><optgroup label="Ingredients">{data.ingredients.map(i=><option key={i.id} value={i.id}>{i.name}</option>)}</optgroup><optgroup label="Bulk Recipes">{data.recipes.filter(r=>r.recipe_type==='bulk'&&r.id!==recipe.id).map(r=><option key={r.id} value={'bulk:'+r.id}>{r.name}</option>)}</optgroup></select></div><div className="double"><div className="form-field"><label>Quantity</label><input id="qty" type="number" step="0.01"/></div><div className="form-field"><label>Unit</label><select id="unit"><option>g</option><option>kg</option><option>ml</option><option>L</option><option>pc</option><option>portion</option></select></div></div><button className="entry-primary" onClick={addComp}>＋ Add to recipe</button><button className="text-link" onClick={()=>setAddingIngredient(true)}>Ingredient missing? Add it now</button></div></>}{step==='method'&&<><div className="section-kicker">STEP 3 OF 4</div><h2>How is it made?</h2><p className="section-copy">Record the method in the exact order another chef should follow.</p><div className="timer"><div><span>LIVE KITCHEN TIMER</span><b>{fmt(elapsed)}</b></div><div>{timerStart?<button onClick={()=>setTimerStart(null)}>Pause</button>:<button onClick={()=>setTimerStart(Date.now()-elapsed*1000)}>Start / Resume</button>}<button onClick={()=>{setTimerStart(null);setElapsed(0)}}>Reset</button><button onClick={()=>setRecipe({...recipe,cook_time_minutes:Math.max(1,Math.round(elapsed/60))})}>Use as cook time</button></div></div><div className="components">{recipe.steps.map((s,n)=><div className="component-row method" key={n}><i>{n+1}</i><div><b>{s.instruction}</b><span>{s.duration_seconds?`${s.duration_seconds} sec`:'No time set'}</span></div><button onClick={()=>setRecipe({...recipe,steps:recipe.steps.filter((_,i)=>i!==n)})}>×</button></div>)}</div><div className="entry-card"><div className="form-field"><label>Instruction</label><input id="methodText"/></div><div className="form-field"><label>Step time (seconds)</label><input id="methodSecs" type="number"/></div><button className="entry-primary" onClick={addStep}>＋ Add step</button></div></>}{step==='finish'&&<><div className="section-kicker">STEP 4 OF 4</div><h2>Finish the kitchen standard</h2><p className="section-copy">Record yield, timing, photos and final notes.</p><div className="double"><div className="form-field"><label>Usable Yield</label><input type="number" value={recipe.yield_quantity||''} onChange={e=>setRecipe({...recipe,yield_quantity:e.target.value})}/></div><div className="form-field"><label>Unit</label><select value={recipe.yield_unit||'g'} onChange={e=>setRecipe({...recipe,yield_unit:e.target.value})}><option>g</option><option>kg</option><option>ml</option><option>L</option><option>portion</option></select></div></div><div className="double"><div className="form-field"><label>Prep minutes</label><input type="number" value={recipe.prep_time_minutes||''} onChange={e=>setRecipe({...recipe,prep_time_minutes:e.target.value})}/></div><div className="form-field"><label>Cook minutes</label><input type="number" value={recipe.cook_time_minutes||''} onChange={e=>setRecipe({...recipe,cook_time_minutes:e.target.value})}/></div></div><div className="photo-grid">{photoTypes.map(([t,l])=>{const p=recipe.photos.find(x=>x.photo_type===t);return <label className="photo" key={t}>{p?<img src={p.public_url} alt=""/>:<><span>＋</span><b>{l}</b></>}<input type="file" accept="image/*" capture="environment" onChange={e=>addPhoto(e,t)}/></label>})}</div><div className="form-field"><label>Kitchen Notes</label><textarea value={recipe.kitchen_notes||''} onChange={e=>setRecipe({...recipe,kitchen_notes:e.target.value})}/></div><div className="form-field"><label>Status</label><select value={recipe.status||'draft'} onChange={e=>setRecipe({...recipe,status:e.target.value})}><option value="draft">Draft</option><option value="recorded">Recipe Recorded</option><option value="approved">Approved Standard</option></select></div></>}</div></div><footer className="editor-footer"><button disabled={idx===0} onClick={()=>setStep(wizard[Math.max(0,idx-1)][0])}>← Back</button>{idx<wizard.length-1?<button className="continue" onClick={()=>setStep(wizard[idx+1][0])}>Continue →</button>:<button className="continue" onClick={()=>saveRecipe()} disabled={saving}>{saving?'Saving…':'Save Recipe'}</button>}</footer></section></main>{addingCategory&&<CategoryModal/>}{addingIngredient&&<IngredientModal/>}</div>}

 function CategoryModal(){return <div className="modal"><header><button onClick={()=>setAddingCategory(false)}>‹</button><div><small>MASTER DATA</small><h2>Add Category</h2></div></header><form onSubmit={saveCategory}><div className="form-field"><label>Category name</label><input name="name" required autoFocus placeholder="e.g. Mocktails"/></div><div className="form-field"><label>Description <small>(optional)</small></label><textarea name="description"/></div><button>Save Category</button></form></div>}
 function IngredientModal(){return <div className="modal"><header><button onClick={()=>setAddingIngredient(false)}>‹</button><div><small>MASTER DATA</small><h2>Add Ingredient</h2></div></header><form onSubmit={saveIngredient}><div className="form-field"><label>Name</label><input name="name" required/></div><div className="double"><div className="form-field"><label>Default unit</label><select name="default_unit"><option>g</option><option>kg</option><option>ml</option><option>L</option><option>pc</option></select></div><div className="form-field"><label>Type</label><select name="ingredient_type"><option value="raw">Raw</option><option value="prepared">Prepared</option></select></div></div><button>Save Ingredient</button></form></div>}

 return <div className="layout"><Sidebar/><main className="main-shell"><div className="page-area">{loading?<div className="empty">Loading kitchen data…</div>:<>{view==='home'&&<><section className="home-hero"><span>SALTED GRILLS RECIPE LAB</span><h1>Build recipes your kitchen can repeat.</h1><p>Capture the process first. Cost it when you are ready.</p><button onClick={()=>startRecipe('menu')}>＋ Start Recipe</button></section><div className="metrics"><div><b>{data.ingredients.length}</b><span>Ingredients</span></div><div><b>{data.recipes.length}</b><span>Recipes</span></div><div><b>{data.recipes.filter(r=>r.recipe_type==='bulk').length}</b><span>Bulk Recipes</span></div><div><b>{data.recipes.filter(r=>r.status==='approved').length}</b><span>Approved</span></div></div><div className="section-title"><div><small>RECENT</small><h2>Continue working</h2></div><button onClick={()=>navRecipes('all')}>See all</button></div><div className="list">{data.recipes.slice(0,6).map(recipeCard)}</div></>}{view==='recipes'&&<><div className="page-head"><div><small>RECIPE LIBRARY</small><h1>{titles[filter][0]}</h1><p>{titles[filter][1]}</p></div><button onClick={()=>startRecipe(filter==='bulk'?'bulk':'menu')}>＋ New Recipe</button></div><div className="search"><span>⌕</span><input value={q} onChange={e=>setQ(e.target.value)} placeholder={`Search ${titles[filter][0].toLowerCase()}`}/></div><div className="list">{filteredRecipes.length?filteredRecipes.map(recipeCard):<div className="empty"><b>No recipes found</b><span>There are no recipes in this section yet.</span></div>}</div></>}{view==='ingredients'&&<><div className="page-head"><div><small>MASTER DATA</small><h1>Ingredients</h1><p>Reusable raw and prepared ingredients.</p></div><button onClick={()=>setAddingIngredient(true)}>＋ Add Ingredient</button></div><div className="search"><span>⌕</span><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search ingredients"/></div><div className="ingredient-grid">{filteredIngredients.map(i=><div className="ingredient" key={i.id}><div>{i.name[0]}</div><p><b>{i.name}</b><span>{i.default_unit} · {i.latest_price?'Price available':'No price yet'}</span></p><button onClick={()=>setPriceItem(i)}>₨</button></div>)}</div></>}{view==='categories'&&<><div className="page-head"><div><small>MASTER DATA</small><h1>Categories</h1><p>Create categories once and select them while building recipes.</p></div><button onClick={()=>setAddingCategory(true)}>＋ Add Category</button></div><div className="search"><span>⌕</span><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search categories"/></div><div className="category-grid">{filteredCategories.map(c=><div className="category-card" key={c.id}><div>◫</div><p><b>{c.name}</b><span>{c.description||'Recipe category'}</span></p></div>)}</div></>}{view==='costing'&&<><div className="page-head"><div><small>COSTING</small><h1>Ingredient Prices</h1><p>Maintain purchase prices separately from kitchen capture.</p></div></div><div className="notice"><b>Costing stays optional.</b><span>Kitchen recipes can be completed before any purchase price is entered.</span></div><div className="ingredient-grid">{data.ingredients.map(i=><div className="ingredient" key={i.id}><div>₨</div><p><b>{i.name}</b><span>{i.latest_price?`Rs ${Number(i.latest_price.purchase_price).toLocaleString()} / ${i.latest_price.purchase_quantity} ${i.latest_price.purchase_unit}`:'No purchase price'}</span></p><button onClick={()=>setPriceItem(i)}>{i.latest_price?'Edit':'Add'}</button></div>)}</div></>}</>}</div></main>{addingCategory&&<CategoryModal/>}{addingIngredient&&<IngredientModal/>}{priceItem&&<div className="modal"><header><button onClick={()=>setPriceItem(null)}>‹</button><div><small>COSTING</small><h2>{priceItem.name}</h2></div></header><form onSubmit={savePrice}><div className="double"><div className="form-field"><label>Purchase quantity</label><input name="purchase_quantity" type="number" step="0.01" required/></div><div className="form-field"><label>Unit</label><select name="purchase_unit"><option>kg</option><option>g</option><option>L</option><option>ml</option><option>pc</option></select></div></div><div className="form-field"><label>Purchase price</label><input name="purchase_price" type="number" step="0.01" required/></div><div className="form-field"><label>Supplier</label><input name="supplier"/></div><button>Save Price</button></form></div>}</div>
}
